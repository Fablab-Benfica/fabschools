[build]
  publish = "exampleSite/public"
  command = "cd exampleSite && hugo --gc --themesDir ../.."
  #command = "cd exampleSite && hugo --gc -b $DEPLOY_PRIME_URL --themesDir ../.."
  
[build.environment]
  HUGO_VERSION = "0.69.0"
  HUGO_THEME = "repo"
